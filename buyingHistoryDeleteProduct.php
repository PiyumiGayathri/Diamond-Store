<?php
// session_start();
// require "connection.php";

// $product_id = $_GET["id"];

// Database::iud("DELETE `invoice` FROM `invoice` INNER JOIN `feedback` On invoice.id = feedback.invoice_id
// INNER JOIN `feedback_img` ON feedback.id=feedback_img.feedback_id WHERE `product_id`='".$product_id."' 
// AND invoice.user_email='".$_SESSION["user"]["email"]."'");

// echo "One Record Deleted!";

?>